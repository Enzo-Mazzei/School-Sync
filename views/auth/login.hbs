<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>SchoolSync - Log in</title>
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0"
    />
    <link rel="stylesheet" href="/stylesheets/reset.css" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="stylesheet" href="/stylesheets/auth.css">
  </head>
<body>
  <div class="auth-container">
  <form class="auth-form"action="/login" method="POST">
    <h1 class="auth-form__title">Log In</h1>
    <div class="auth-form__group {{#if errorEmail}}auth-form__group-error{{/if}}">
      <input class="auth-form__input" type="text" placeholder=" " name="email" value="{{email}}"/>
      <label class="auth-form__label">Email adress</label>
    </div>
    {{#if errorEmail}}
      <p class="auth-form__error-container">
        <span class="auth-form__error-icon material-symbols-outlined">error</span>
        <span class="auth-form__error-text">{{errorMessage}}</span>
      </p>
    {{/if}}
    <div class="auth-form__group {{#if errorPassword}}auth-form__group-error{{/if}}">
      <input class="auth-form__input" type="password" placeholder=" " name="password" value="{{password}}" />
      <label class="auth-form__label">Password</label>
    </div>
    {{#if errorPassword}}
      <p class="auth-form__error-container">
        <span class="auth-form__error-icon material-symbols-outlined">error</span>
        <span class="auth-form__error-text">{{errorMessage}}</span>
      </p>
    {{/if}}
    <button class="auth-form__submit-button" type="submit">
      <span class="material-symbols-outlined auth-form__submit-button-icon">
        login
      </span>
      <span class="auth-form__submit-button-text">Continue</span>
    </button>
    <div class="auth-form__footer-container">
      <p class="auth-form__footer-text">Don't have an account? <a href="/signup">Sign up</a></p>
      <p class="auth-form__footer-text">Go back to <a href="/">home page</a></p>
    </div>
  </form>
</div>
</body>
</html>